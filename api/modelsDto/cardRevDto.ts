export default interface CardRevDto {
    id: string;
    reviewDate: string;
    lastDaysToReview: string;

    // constructor(id, reviewDate, lastDaysToReview) {
    //     this.id = id;
    //     this.reviewDate = reviewDate;
    //     this.lastDaysToReview = lastDaysToReview;
    // }
}